import { StyleSheet } from 'react-native';
const styles = StyleSheet.create({
  container: {
    marginHorizontal: 20,
  },
  nameText: {
    fontSize: 12,
    lineHeight: 24,
    fontWeight: '500',
    marginTop: 4,
  },
});

export default styles;
